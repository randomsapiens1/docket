import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Docket | Holding Tax Assessment & Payment',
  description: 'Step-by-step guide for Holding Tax Assessment, Registration, and Payment in Bangladesh.',
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
